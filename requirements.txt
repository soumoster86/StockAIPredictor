# =============================
# requirements.txt
# (deploy-ready for Streamlit Community Cloud)
# =============================
# CPU-only PyTorch — see DEPLOYMENT.md. Requires Python <= 3.12.
# On macOS, if the +cpu pin fails locally, replace the two torch lines
# with:  torch~=2.5
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

# Pinned with ~= (compatible release): allows patch updates, blocks
# breaking minor/major bumps. yfinance especially changes behavior across
# minor versions, so it's pinned tightest. Bump these deliberately, then
# let CI (pytest) confirm nothing broke before you deploy.
streamlit~=1.40
pandas~=2.2
numpy~=1.26
yfinance==0.2.51
scikit-learn~=1.5
joblib~=1.4
xgboost~=2.1
plotly~=5.24
