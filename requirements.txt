# =============================
# requirements.txt
# (deploy-ready for Streamlit Community Cloud)
# =============================
# CPU-only PyTorch from the official wheel index.
# 2.5.1+cpu was removed from the index; use a current +cpu build.
# Works on Python 3.11–3.14 (use 3.12 on Streamlit Cloud if available).
# If +cpu fails on your platform, install:  pip install torch
# (or replace the two torch lines with:  torch>=2.9,<3 )
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu

# Pinned with ~= (compatible release): allows patch updates, blocks
# breaking minor/major bumps. Bump deliberately; let CI (pytest) confirm
# nothing broke before deploying.
streamlit~=1.40
pandas~=2.2
# numpy 1.26 has no wheels for Python 3.13+; 2.x is required on 3.14
numpy>=1.26,<3
yfinance~=1.4
curl_cffi~=0.7
scikit-learn~=1.5
joblib~=1.4
xgboost~=2.1
plotly~=5.24
reportlab~=4.2
